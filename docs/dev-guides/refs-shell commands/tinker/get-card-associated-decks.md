For a given $card variable where card has deck, retrieve the attached decks through the decks function `decks()` defined by the Card model function`function decks()`.

```sh tinker
$card->decks()->get();
```
