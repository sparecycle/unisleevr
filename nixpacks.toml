[phases.setup]
nixPkgs = ["php", "nodejs_18", "sqlite"]

[phases.install]
cmds = ["composer install --no-dev", "npm ci"]

[phases.build] 
cmds = ["npm run build"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"